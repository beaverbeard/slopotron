---
description: Почистить русскоязычный текст от AI-маркеров и мёртвых паттернов
argument-hint: [текст или путь к файлу] (или просто вставь текст следующим сообщением)
---

Используй скил `antislop`. Если в запросе явно сказано «только флаги» / «detect» —
работай в режиме Detect (только список проблем). Иначе — Fix: найди AI-маркеры,
покажи таблицу флагов и отдай очищенный текст.

$ARGUMENTS
